# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /distance/:id1/:id2         controllers.Controller.getDistance(id1: Long, id2: Long)
GET     /common_friends/:id1/:id2   controllers.Controller.getCommonFriends(id1: Long, id2: Long)